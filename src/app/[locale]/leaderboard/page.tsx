import LeaderBoard from '@/features/Leaderboard'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <LeaderBoard />
        </div>
    )
}

export default page