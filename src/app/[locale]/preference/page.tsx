import Preferences from '@/features/Preferences'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Preferences />
        </div>
    )
}

export default page