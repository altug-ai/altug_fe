import Coach from '@/features/Coach'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Coach />
        </div>
    )
}

export default page