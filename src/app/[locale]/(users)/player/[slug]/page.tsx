import Player from '@/features/Player'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Player />
        </div>
    )
}

export default page