import Wallet from '@/features/Wallet'
import React from 'react'

type Props = {}

const page = (props: Props) => {
    return (
        <div>
            <Wallet />
        </div>
    )
}

export default page